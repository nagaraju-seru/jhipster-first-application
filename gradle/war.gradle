apply plugin: "war"

bootWar {
    mainClassName = "io.github.jhipster.application.JhipsterpractiseApplicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    webAppDirName = "build/resources/main/static/"
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
